# Example environment file — copy to .env and fill in real values.
# .env is git-ignored and Claude Code is denied from reading it.
# Never put real keys in this file.

# Used by the AI Puzzle Studio's real LLM transport (studio/llm.js).
# The Studio runs on the mock transport until this is set.
# ANTHROPIC_API_KEY=
