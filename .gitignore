# Secrets — never committed
.env
.env.*
!.env.example

# Dependencies & build output
node_modules/
dist/
build/

# OS junk
.DS_Store
Thumbs.db

# Local-only files
*.local

# Art pipeline: the manual-render handoff is scratch, never content (D-31)
art/pending/
